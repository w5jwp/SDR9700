# Claude Code Project Guide for SDR9700

Claude Code should read `AGENTS.md` first. That file is the canonical project
guide for AI assistants.

Coding rules live in `CONVENTIONS.md`.

This file exists only because Claude Code auto-loads `CLAUDE.md`.
